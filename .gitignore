# Ambiente virtual — não versionar
.env
.venv/
venv/

# Bytecode Python
__pycache__/
*.pyc

# Arquivos de swap de editor
.*.swp
.*.swo

# ==== Itens pré-reorganização (2026-07-15) ====
# Estes arquivos/pastas na raiz são a versão ANTIGA (achatada) do repositório,
# mantida em disco só até a revisão de docs/RELATORIO_REORGANIZACAO.md — o
# conteúdo equivalente já foi copiado para data/, src/, results/, docs/ e
# environment/. Depois de confirmar o relatório e apagar estes itens da
# raiz, remova também estas linhas do .gitignore.