# Pacotes R necessários para src/analysis/geracao_graficos.R
# Versões registradas em 2026-07-15 (R 4.6.0), via:
#   Rscript -e 'for (p in c("ggplot2","dplyr","tidyr")) cat(p, as.character(packageVersion(p)), "\n")'
#
# Instalação:
#   Rscript -e 'install.packages(c("ggplot2", "dplyr", "tidyr"))'
#
# Não usamos renv (nenhum renv.lock pré-existia neste projeto e o script
# depende de só 3 pacotes de CRAN).
ggplot2==4.0.3
dplyr==1.2.1
tidyr==1.3.2
